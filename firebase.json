{
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint",
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ],
    "source": "packages/functions",
    "ignore": [
      "firebase.json",
      ".firebaserc",
      ".firebase/**",
      "**/node_modules/**"
    ]
  }
}
